# Milestone 1 is offline and does not require environment variables.
# Future milestones may add provider and notification configuration here.
WEATHER_PROVIDER=open-meteo
DATABASE_URL=sqlite:///weather_coach.sqlite3
